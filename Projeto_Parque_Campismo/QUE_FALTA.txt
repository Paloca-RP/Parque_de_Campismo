-> mapa lotes bugado nao sei o que fazer da vida (ass.Paloca)

-->comentários(o que entra/input-output) antes de cada função

--> modify lotes

--> editar lote some shitt reservar lote

--> pesquisa por nome(2 ciclo um para linha outro para coluna i usar o strcmp)


{
    
    -> fazer "for" que percorre o array da  struct para inserir num ficheiro no final do programa
    -> le o ficheiro quando abre o programa 
    -> se houver vagar(
        histórico de reservas;)
}
